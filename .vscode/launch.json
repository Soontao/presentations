{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Marp Server",
      "program": "${workspaceFolder}/node_modules/.bin/marp",
      "request": "launch",
      "args": [
        "-s",
        "src"
      ],
      "skipFiles": [
        "<node_internals>/**"
      ],
      "type": "node"
    }
  ]
}