

/**
 * create user
 * 
 * @param {string} name 
 * @param {number} value
 * @returns {import("./class")} user instance
 */
const f = (name, value) => {

}

/**
 * @param {{a:string,b:{c:number}}[]} arr 
 */
const f2 = (arr = []) => {
    arr[0].b.c.toFixed(2)
}

module.exports = f